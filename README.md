# End-to-end analytics platform

Project Overview
This project simulates a real e-commerce analytics system. It ingests user events in real time, stores raw data in a data lake, processes it using batch jobs, and exposes analytics-ready tables with automated scheduling and data quality checks.
